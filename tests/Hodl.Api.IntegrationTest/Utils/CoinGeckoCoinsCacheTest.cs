namespace Hodl.Api.Utils;

public class CoinGeckoCoinsCacheTest
{
    /*
    [Test]
    [TestCase("BTC", "bitcoin")]
    [TestCase("btc", "bitcoin")]
    [TestCase("Btc", "bitcoin")]
    [TestCase("ETH", "ethereum")]
    public async Task GetCoinId(string symbol, string expectedId)
    {
        //CoinGeckoApi cgCoins = new();
        //Assert.AreEqual(expectedId, await cgCoins.GetCoinId(symbol));

        // TODO: refactor test for market api CoinGecko

        Assert.Ignore();
    }
    */
}
